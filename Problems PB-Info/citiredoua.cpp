#include <bits/stdc++.h>
using namespace std;
int main() {
  long long sum = 0;
  int a, u;
  while (a != u ) {
    a = u;
    cin >> u;
    sum = sum + u;
  }
  cout << sum;
  return 0;
}
